function createTitle(titleName) {
  let title = `The ${titleName}`
  return title
}

module.exports = {
  createTitle,
  buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}